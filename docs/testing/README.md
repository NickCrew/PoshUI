# Testing

Run `pwsh ./build.ps1 -Task test` for Pester alone or `pwsh ./build.ps1 -Task check` for the complete local gate. The detailed suite and benchmark reference is in [Testing the PowerShell port](TESTING.md).
